import styled from 'styled-components';

const WrapMetaContent = styled.div`
  border: 1px solid rgb(221, 221, 221);
  border-radius: 2px;
  background: rgb(250, 250, 250);
  margin-top: 2em;
  font-family: Open Sans, Helvetica, sans-serif;
  font-size: 16px;
`;

export default WrapMetaContent;
